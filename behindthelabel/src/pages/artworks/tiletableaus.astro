---
import Subtitle from "../../components/Subtitle.astro";
import SpeechBubble from "../../components/Speechbubble.astro";
import Layout from "../../layouts/Layout.astro";
import { Image } from "astro:assets";
import tile from "../../assets/tiletableau.png";
---

<Layout>
    <div class="detail">
        <div class="start">
            <h2 class="start__subtitle">
                What is hiding behind the labels of Tegeltableaus?
            </h2>
            <Image class="tile" src={tile} alt="tegeltableaus" />
            <p class="start__text">Shake the labels off</p>
            <div class="corner-square"></div>
        </div>
        <div class="story hidden">
            <h2 class="story__subtitle top">These artworks tell</h2>
            <h2 class="story__subtitle bottom">
                the stories of <span class="blue bold">Aalbeke</span>
            </h2>
            <div class="image-wrapper">
                <Image class="image" src={tile} alt="tegeltableaus" />
            </div>
            <p class="story__text">
                Created by <span class="bold">Peter de Graaff</span>, he
                listened to stories from locals dug through the village’s
                history. They reflect what it was like to live and work in the
                village, both in the past and today. The stories are about the
                church fire, working in agriculture or the textile factory,
                Saint Cornelius and more.
            </p>
            <h2 class="story__subtitle top">12 ceramic tiles are placed</h2>
            <h2 class="story__subtitle bottom">
                along the <span class="blue bold">sidewalks</span>
            </h2>
            <p class="story__text">
                He took all those stories and turned them into art. In total,
                there are <span class="bold">12 ceramic tiles</span> with drawings
                spread throughout the village. You’ll find them placed in the ground
                along the <span class="bold">sidewalks</span>, each one marking
                a small resting point in the landscape.
            </p>
            <h2 class="story__subtitle top blue bold">Hugo Claus,</h2>
            <h2 class="story__subtitle bottom">the famous writer</h2>
            <p class="story__text">
                One of the scenes shows a young boy writing, and fun fact: that
                boy turned out to be <span>Hugo Claus</span>, the famous writer.
                He spent some time living in a <span class="bold"
                    >boarding school</span
                > in Aalbeke.
            </p>
        </div>
    </div>

    <style>
        .detail {
            position: relative;
            height: fit-content;
        }

        .start {
            background-color: var(--black);
            display: flex;
            flex-direction: column;
            align-items: center;
            height: auto;
            position: relative;
        }
        .start__subtitle {
            color: var(--white);
            font-size: 1.5rem;
            font-weight: 600;
            text-align: center;
            line-height: 170%;
            padding: 3rem 0rem;
        }

        .start__subtitle::after {
            content: "Tegeltableaus";
            background-color: var(--secondary-color);
            display: flex;
            justify-self: end;
            width: fit-content;
            padding: 0.2rem 0.8rem;
            color: var(--black);
            border-radius: 3rem;
            margin-right: 4rem;
            margin-top: -2rem;
            rotate: -3deg;
            font-size: 1.25rem;
        }

        .tile {
            border: 0.5rem solid var(--white);
            display: flex;
            justify-self: center;
        }

        .start__text {
            font-size: 1.25rem;
            font-weight: 500;
            color: var(--white);
            padding: 4rem;
        }

        .corner-square {
            width: 5rem;
            height: 5rem;
            background: linear-gradient(
                to top left,
                var(--primary-color) 50%,
                var(--secondary-color) 50%
            );
            bottom: 0;
            right: 0;
            position: absolute;
        }

        .corner-square::before {
            content: "";
            width: 4.5rem;
            height: 4.5rem;
            background: url("../../assets/icon/wiggle.svg") no-repeat
                center/contain;
            display: flex;
            margin-top: -2rem;
            margin-left: -2rem;
        }

        .story {
            position: relative;
            margin-top: 3rem;
            background: url("../../assets/blabla.svg") no-repeat center/contain;
        }

        .story__subtitle {
            background-color: var(--white);
            font-weight: 500;
            padding: 0.8rem;
            text-align: center;
            font-size: 1.25rem;
            margin: 1rem 0rem;
        }
        .top {
            rotate: 3deg;
            margin-top: 5rem;
        }

        .story__text {
            border: 0.1rem solid var(--black);
            background-color: var(--white);
            padding: 1rem;
            border-radius: 0rem 2.5rem 2.5rem 2.5rem;
            max-width: 23rem;
            margin: 2rem 0.8rem;
            line-height: 150%;
            overflow: hidden;
        }

        .image {
            display: flex;
            justify-self: center;
        }

        .image-wrapper {
            position: relative;
        }

        .image-wrapper::before {
            content: "";
            width: 14rem;
            height: 14rem;
            background: url("../../assets/speak.svg") no-repeat center/contain;
            position: absolute;
            margin-top: -4rem;
            margin-left: -2rem;
            pointer-events: none;
        }

        .image-wrapper::after {
            content: "";
            width: 20rem;
            height: 20rem;
            background: url("../../assets/ear.svg") no-repeat center/contain;
            position: absolute;
            margin-left: 8rem;
            margin-top: -7rem;
            pointer-events: none;
        }

        .story {
            position: relative; /* of sticky/absolute als nodig */
            z-index: 1;
        }
        .start {
            z-index: 2; /* zodat start boven story staat tot shake */
        }
    </style>
</Layout>
